## 1. Directory Structure Setup

- [x] 1.1 Create the top-level `deployments` folder
- [x] 1.2 Move existing `supabase/` folder to `deployments/supabase/`
- [x] 1.3 Move existing `terraform/` folder to `deployments/terraform/`
- [x] 1.4 Create empty placeholder folder `deployments/ansible/`

## 2. Core Application Layout Setup

- [x] 2.1 Create the top-level `app` folder
- [x] 2.2 Create `app/frontend/` directory structure for the React frontend
- [x] 2.3 Create `app/backend/` directory structure for the Spring Boot backend
- [x] 2.4 Create `.github/workflows/` directory at the repository root

## 3. Tooling and Verification

- [x] 3.1 Verify git tracks all moved files under `deployments/supabase/` and `deployments/terraform/`
- [x] 3.2 Run graphify update command to sync the codebase index
